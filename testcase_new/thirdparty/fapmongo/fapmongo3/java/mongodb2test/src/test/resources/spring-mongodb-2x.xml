<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mongo="http://www.springframework.org/schema/data/mongo"
       xmlns="http://www.springframework.org/schema/beans"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
		http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context
		http://www.springframework.org/schema/context/spring-context.xsd
		http://www.springframework.org/schema/data/mongo
		http://www.springframework.org/schema/data/mongo/spring-mongo-1.4.xsd">

    <context:property-placeholder location="classpath*:mongodb.properties"/>
    <context:component-scan base-package="com.mongodb.*" />

    <mongo:mongo id="mongo" host="${mongodb.host}" port="${mongodb.port}">
        <mongo:options connections-per-host="${mongodb.connectionsPerHost}"
                       threads-allowed-to-block-for-connection-multiplier="${mongodb.threadsAllowedToBlockForConnectionMultiplier}"
                       connect-timeout="${mongodb.connectTimeout}"
                       max-wait-time="${mongodb.maxWaitTime}"
                       auto-connect-retry="${mongodb.autoConnectRetry}"
                       socket-keep-alive="${mongodb.socketKeepAlive}"
                       socket-timeout="${mongodb.socketTimeout}"
                       slave-ok="${mongodb.slaveOk}"
                       write-number="${mongodb.writeNumber}"
                       write-timeout="${mongodb.writeTimeout}"
                       write-fsync="${mongodb.writeFsync}"/>
    </mongo:mongo>

    <!-- mongo的工厂，通过它来取得mongo实例,dbname为mongodb的数据库名，没有的话会自动创建  -->
    <mongo:db-factory dbname="${mongodb.dbname1}" mongo-ref="mongo"
                      id="mongoDbFactory"  username="" password=""/>
    <mongo:mapping-converter id="converter" />

    <!-- mongodb的主要操作对象，所有对mongodb的增删改查的操作都是通过它完成 -->
    <bean id="mongoTemplate" class="org.springframework.data.mongodb.core.MongoTemplate">
        <constructor-arg name="mongoDbFactory" ref="mongoDbFactory"/>
    </bean>

    <bean name="gridFsTemplate"
          class="org.springframework.data.mongodb.gridfs.GridFsTemplate">
        <constructor-arg ref="mongoDbFactory" />
        <constructor-arg ref="converter" />
        <constructor-arg type="String" value="bucket"/>
    </bean>

    <bean id="config" class="com.mongodb.utils.Config">
        <constructor-arg type="String" value="${mongodb.host}"/>
        <constructor-arg type="Integer" value="${mongodb.port}"/>
        <constructor-arg type="String" value="${mongodb.dbname1}"/>
        <constructor-arg type="String" value="${mongodb.dbname2}"/>
        <constructor-arg type="String" value="${mongodb.username}"/>
        <constructor-arg type="String"  value="${mongodb.password}"/>
    </bean>

</beans>
