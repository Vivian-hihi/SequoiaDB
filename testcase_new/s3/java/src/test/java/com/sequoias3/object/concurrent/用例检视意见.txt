CreateSameObject16484:
1.CreateObjectThread类中可以不需要传入keyName，keyName已经定义为全局变量，可以直接使用
2、检查结果中建议补充验证下插入对象内容正确性。

UpdateSameObject16485
1、keyName已定义全局变量，不需要做为参数再次传入到并发线程中

GetObjectList16486：
1、62-63行：keyName需要带上用例Id标志

GetSameObject16488：
1、检查结果需要补充验证获取对象内容信息

CreateAndDelectSameObject16490：
1、102行：用例中不要用sleep，如果有特殊场景必须要sleep，需要说明；该用例是不需要sleep的
2、keyName已定义全局变量，不需要再次作为参数传入并发线程中
3、需要补充创建成功，删除失败的结果检测

UpdateAndGetObject16493：
1、需要补充获取对象结果验证

GetObjectListWithVersion16499：
1、63-64行：检查结果需要补充验证获取对象内容信息

GetSameObjectWithVersion16500：
1、GetObjectThread类中：getObject需要读取对应的文件内容，防止db端游标未关闭；其他并发线程类也存在此问题
2、验证多个版本的对象内容，建议不同版本上传不同内容，不要都写同一个内容

DeleteSameObjectWithVersion16501：
1、同一对象的每个版本的内容一致，删除其中一个版本对象，对应的版本id也会变，无法比较删除结果；建议用不同内容区分

CreateAndDelectSameObject16503：
1、同用例16490，用例中不要用sleep，如果有特殊场景必须要sleep，需要说明；该用例是不需要sleep的
2、需要补充删除失败，创建成功的结果


UpdateAndDelectSameObject16504：
1、用例中无特殊场景不要用sleep
2、测试点需要补充检测点：1、删除成功，更新成功；删除可能删除更新的最新版本对象，需要校验原对象未删除


DeleteAndGetSameObject16505：
1、这个用例的测试点需要优化，测试结果覆盖不同的场景
2、getObject没有读取对象，可能导致db游标未及时关闭

DeleteAndGetSameObject16506：
1、check中getObject没有读取对象，可能导致db游标未及时关闭
2、检查点覆盖不全：删除成功，获取对象成功（需要校验获取历史版本中移动过来的对象）

GetSameObjectWithoutVersion16511：
1、建议校验获取的对象内容，不同版本对象内容不同

CreateAndDelectSameObject16512：
1、98行：用例中不要用sleep，如果有特殊场景必须要sleep，需要说明；该用例是不需要sleep的
2、keyName已定义全局变量，不需要再次作为参数传入并发线程中
