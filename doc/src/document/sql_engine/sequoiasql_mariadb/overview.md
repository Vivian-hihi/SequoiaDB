MariaDB 是一款开源的关系型数据库管理系统，它是 MySQL 的一个分支，主要由开源社区维护。它完全支持标准的 SQL 语言，完全兼容 MySQL，包括 API 和命令行。
SequoiaDB 支持创建 MariaDB 实例，完全兼容 MariaDB 语法和协议，用户可以使用 SQL 语句访问 SequoiaDB 数据库，完成对数据的增、删、查、改操作以及其他 MariaDB 语法操作。

**SequoiaDB 所支持的 MariaDB 版本** 

- MariaDB 10.4.6+
