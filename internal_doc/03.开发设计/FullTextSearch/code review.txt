0913：
1. adapter 使用动态端口；不要以0结尾，可在数据结点上 +7 开始尝试
2. 日志放到sequoiadb/conf/log下，用端口号生成对应目录存放；日志需要设置使用上限
3. not + 全文检索条件有问题，看看能否一次取完，如果不行则报错。（10W）
4. 固定集合被删除时，清理索引，有没有可能只是临时查不到集合。
5. 索引名中加上索引逻辑ID，怎么解决重建一个同名索引的问题？
6. 新增加的几个消息，优先使用已存在的消息类型。
7. 引擎回消息给adapter时，通过netid返回，不通过 route id 返回，类似数据节点给coord回消息
8. 对数据节点主备的检测，不使用单独的信息，每个消息中，可通过设置 flag，让它只在主上可执行。
9. SYSmegacorpemployeeidx_1  需要加_ 分开，否则可能重名，ES上的索引也要考虑这个问题
10. es上的类型名用组名，不用ID
11. pop 保留增加一个条件：保留 4k 数据。
12. 在适配器上缓存一份索引信息，保存 cs-cl-index(include definition)，来了查询请求时，根据集合名查找索引，
    如果多个且没有hint则报错。
13. 当前在向固定集合中插入时，是奖原始记录中的 _id 固定识别为OID类型，这在用户指定_id时会出错，需要讨论如何处理。
